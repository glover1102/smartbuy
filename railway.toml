[build]
builder = "nixpacks"
buildCommand = "pnpm install && prisma generate && prisma migrate deploy && next build"

[deploy]
startCommand = "node .next/standalone/server.js"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
